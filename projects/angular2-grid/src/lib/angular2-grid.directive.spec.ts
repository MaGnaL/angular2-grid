import { Angular2GridDirective } from './angular2-grid.directive';

describe('Angular2GridDirective', () => {
  it('should create an instance', () => {
    const directive = new Angular2GridDirective();
    expect(directive).toBeTruthy();
  });
});
